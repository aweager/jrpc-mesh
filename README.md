# jrpc-mesh

Simple JSON RPC service mesh and reverse proxy intended for local dev box services.

## Repo Layout

Developer documentation lives in the `./doc` folder.

JSON schemas live under `./api/json-schema`
